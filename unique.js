/*var arr = ['a', 'b', 'c', 'd', 'a', 'c'];
var newArr = [];
for (var i = 0; i < arr.length; i++){
    for (var j = i; j < arr.length; j++){
        if (arr[i] === arr[j]&&i!=j) {
            //console.log(i, j);
            newArr.push(arr[i]);
        }
    }
}
console.log(newArr);*/
var obj = {
    arr:['a', 'b', 'c', 'd', 'a', 'c'],
}